<%= render 'listing_menu' %>

<div class="row">
  <div class=" text-center">
    <div class="panel panel-default">
      <h3>商品の詳細</h3>

      <p><b><%= @listing.listing_title %></b></p>

      <div class="container">
       <%= form_for @listing,html: {multipart: true} do |f| %>

        <%= f.hidden_field :location, value: @user.address_city %>

        <div class="form_box">
          <p>商品の詳細説明</p>
          <div class="form-group">
            <%= f.text_area :description, rows: 5, placeholder: "例:昨年の夏に購入しました。湘南の波にピッタリで、上級者からビギナーまで楽しめます。シングルフィン、リシューコード付き、サイズ7'0×22×3 1/8", class: "form-control", required: true %>
          </div>
        </div>

        <div class="form_box">
          <p>利用可能なアメニティ</p>
          <div class="form-group row">
            <div class="col-md-4 col-sm-4 col-xs-4 check">
              <%= f.check_box :is_shower %>
              シャワー
            </div>
            <div class="col-md-4 col-sm-4 col-xs-4 check">
              <%= f.check_box :is_bicycle %>
              自転車
            </div>
            <% if !(@listing.category2 == "ボード置き場") %>
             <div class="col-md-4 col-sm-4 col-xs-4 check">
               <%= f.check_box :is_wetsuit %>
              ウェットスーツ
             </div>
            <% end %>
          </div>
        </div>

        <div class="form_box">
          <p>
            リクエスト&nbsp;or&nbsp;即決<br>
            <span  class="form-explain">リクエスト&nbsp;:&nbsp;ゲストからのリクエストを受けてからレンタルの可否を返答できます<br>
            即決&nbsp;:&nbsp;ゲストが「今すぐ予約」ボタンを押した段階で取引が成立します</span>
          </p>

          <div class="form-group">

            <select selected="selected" class="form-control" name="listing[instant]" id="listing_instant">
              <option selected="selected" value="Request">リクエスト</option>
              <option value="Instant">即決</option>
            </select>

          </div>
        </div>

        <% if !(@listing.category2 == "ボード置き場") %>
         <div class="purchase">
          <p class="text-left">
            ※破損が起きた際の補償費の計算の参考にします
          </p>

          <div class="form_box">
            <p>購入時期</p>
            <div class="form-group">
              <%= f.text_field :purchase_time, placeholder: "例:2016年春ごろ", class: "form-control", required: true %>
            </div>
          </div>

          <div class="form_box">
            <p>購入価格</p>
            <div class="">

              <div class="form-group" >
                <ul class="row">

                  <li class="">
                    <%= f.text_field :purchase_price, placeholder: "例:80000", class: "form-control", required: true %>
                  </li>
                  <li class="">
                    <p>円</p>
                  </li>
                </ul>
              </div>

            </div>

          </div>



          </div>
        </div>
        <% end %>

        <div class="actions">
          <%= f.submit "登録する", class: "btn btn-form" %>
        </div>

       <% end %>
      </div>
    </div>
  </div>
</div>

<script>
  $(function() {
    $("#autoaddress").geocomplete();
  })
</script>
