<!DOCTYPE html>
<html lang="ja">
  <head>
    <!-- Global site tag (gtag.js) - Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=UA-73407962-6"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'UA-73407962-6');
    </script>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <title>サーフ文庫</title>



    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <script type="text/javascript"
src="https://maps.googleapis.com/maps/api/js?key=AIzaSyA7ZRYgtCagsAWTqGbqxkpW09Y1i_ZYixI&libraries=places">
    </script>

    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <!-- Google font読み込み-->
    <link rel="stylesheet" href="//fonts.googleapis.com/earlyaccess/notosansjapanese.css"  type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Racing+Sans+One" rel="stylesheet">

    <!-- Font Awesome読み込み -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">



    <!-- animate.css読み込み -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/animate.css@3.5.2/animate.min.css">


    <!-- pretty-checkbox読み込み -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/pretty-checkbox@3.0/dist/pretty-checkbox.min.css">


  </head>

  <body>
   <div id="wrapper">

    <% if  request.path == '/entry'  || request.path == '/entry/confirm' || request.path == '/entry/thanks' || request.path == '/apply'  || request.path == '/apply/confirm' || request.path == '/apply/thanks' %>
      <div class="">
       <%= yield %>
      </div>
    <% else %>

      <%= render 'shared/navbar'%>
      <%= render 'shared/message'%>

      <% if current_user %>
      <input type="hidden" id="user_id" value="<%= current_user.id %>">
      <% end %>


      <% if   request.path == '/search'%>
       <%= yield %>

      <% elsif request.path == '/' || request.path == '/about' || request.path == '/information' || request.path == '/pages/guide' || request.path == '/index' || request.path == '/refer' || request.path == '/users' %>
       <%= yield %>
       <%= render 'shared/footer'%>

      <% elsif  request.path == '/login' || request.path == '/registration'   %>
       <div class="container adjustment">
         <%= yield %>
       </div>
       <%= render 'shared/footer'%>


      <% else %>
       <div class="container">
        <%= yield %>
       </div>
       <%= render 'shared/footer'%>

      <% end %>

    <% end %>

   </div>
  </body>
</html>
